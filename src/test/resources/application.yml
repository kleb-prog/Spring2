spring:
  datasource:
#    driver-class-name: org.postgresql.Driver
#    jpa:
#      hibernate:
#        ddl-auto: create
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  flyway:
    enabled: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: klebedevspb@gmail.com
    password: myPass
    protocol: smtp
    tls: true
    properties.mail.smtp:
      auth: true
      starttls.enable: true
      ssl.trust: smtp.gmail.com

files:
  storepath:
    icons:
    images:

supershop:
  rabbitmq:
    exchange:
    routingkey:
    queue:

documents:
  service:
    url: